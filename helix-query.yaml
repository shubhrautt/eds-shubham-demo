version: 1
indices:
  test:
    target: /vehicles-index.json
    include:
      - 'fragment/vehicles/**'
    properties:
      title:
        select: head > meta[property="og:title"]
        value: |
          attribute(el, 'content')