version: 1
indices:
  vehicles:
    target: /vehicles.json
    include:
      - 'fragment/vehicles/**'
    properties:
      title:
        select: head > meta[property="og:title"]
        value: |
          attribute(el, 'content')